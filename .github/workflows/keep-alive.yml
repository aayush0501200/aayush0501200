name: keep-alive
on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch: # This single line enables the manual button
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s https://swift-ticket-chi.vercel.app/ > /dev/null
